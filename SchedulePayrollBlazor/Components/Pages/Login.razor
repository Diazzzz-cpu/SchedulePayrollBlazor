@page "/"
@page "/login"

@using System.ComponentModel.DataAnnotations
@inject SchedulePayrollBlazor.Services.AuthService AuthService
@inject NavigationManager Nav

<div class="auth-page">
    <div class="auth-card">
        <div class="auth-header">
            <h2>Sign in to SchedulePayroll</h2>
            <p>Access your scheduling and payroll workspace.</p>
        </div>

        <EditForm Model="model" OnValidSubmit="HandleLogin" FormName="LoginForm">
            <DataAnnotationsValidator />

            <div class="form-group">
                <label>Email Address</label>
                <InputText @bind-Value="model.Email" type="email" placeholder="you@example.com" />
                <ValidationMessage For="() => model.Email" />
            </div>

            <div class="form-group">
                <label>Password</label>
                <InputText @bind-Value="model.Password" type="password" placeholder="••••••••" />
                <ValidationMessage For="() => model.Password" />
            </div>

            @if (!string.IsNullOrWhiteSpace(errorMessage))
            {
                <div class="alert alert-danger">@errorMessage</div>
            }

            <button type="submit" class="btn btn-primary btn-block" disabled="@isLoading">
                @if (isLoading)
                {
                    <span>Signing in...</span>
                }
                else
                {
                    <span>Sign In</span>
                }
            </button>
        </EditForm>

        <div class="auth-footer">
            <p>Only administrators can create employee accounts through the Admin panel.</p>
        </div>
    </div>
</div>

@code {
    private readonly LoginModel model = new();
    private string errorMessage = string.Empty;
    private bool isLoading;

    private async Task HandleLogin()
    {
        errorMessage = string.Empty;
        isLoading = true;

        try
        {
            // IMPORTANT: await and use the injected AuthService instance
            var (success, message, role) = await AuthService.LoginAsync(model.Email, model.Password);

            if (!success)
            {
                errorMessage = string.IsNullOrWhiteSpace(message)
                    ? "Login failed. Please check your credentials."
                    : message;
                return;
            }

            var destination = string.Equals(role, "Admin", StringComparison.OrdinalIgnoreCase)
                ? "/admindashboard"
                : "/userdashboard";

            Nav.NavigateTo(destination, forceLoad: true);
        }
        catch (Exception ex)
        {
            // Show what actually went wrong instead of the generic grey bar
            errorMessage = $"Unexpected error during login: {ex.Message}";
        }
        finally
        {
            isLoading = false;
        }
    }

    private sealed class LoginModel
    {
        [Required]
        [EmailAddress]
        public string Email { get; set; } = string.Empty;

        [Required]
        [MinLength(6)]
        public string Password { get; set; } = string.Empty;
    }
}
